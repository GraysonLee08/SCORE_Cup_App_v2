{
  "name": "@scores-cup/engine",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Pure tournament engine: fixtures, scheduling, standings, feasibility. No database, no HTTP.",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vitest": "^2.1.8"
  }
}
